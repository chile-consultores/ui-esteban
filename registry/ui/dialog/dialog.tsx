// ⚠️ ARCHIVO PENDIENTE — reemplaza todo este contenido.
//
// Pega acá tu Dialog.tsx (el que ya tiene focus trap). Este es tuyo,
// así que no hay tema de licencia: repo público sin problema.
//
// Al pegarlo, revisa dos cosas:
//   - Que no tenga imports con rutas relativas raras (../../lib/x).
//     Usa el alias @/ — el CLI lo reescribe solo al instalar.
//   - Si depende de otro archivo tuyo (ej. cn() en lib/utils), agrégalo
//     como archivo extra en registry/ui/registry.json, o como
//     registryDependencies si vive en otro item.
//
// Después llena el README.md de esta carpeta.

export function Dialog() {
  return null;
}
